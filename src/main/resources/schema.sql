DROP TABLE IF EXISTS User;
CREATE TABLE User(userid int NOT NULL AUTO_INCREMENT PRIMARY KEY, fistname VARCHAR(50) NOT NULL, lastname VARCHAR(50) NOT NULL, username VARCHAR(50) NOT NULL, email VARCHAR(50) NOT NULL, password VARCHAR(255) NOT NULL);